#EXERCÍCIO 4:

dist = float(input("Distância desejada em km: "))

if dist <= 200:
    print(f"O preço cobrado em sua viagem será de {dist * 0.5}")
else:
    print(f"O preço cobrado em sua viagem será de {dist * 0.45}")
